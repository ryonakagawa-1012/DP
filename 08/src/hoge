/*name : Cat2.javaabout : 第８講 練習問題8-1 行番号付きのCatコマンドの作成        https://ksuap.github.io/2022autumn/lesson08/assignments/#1-行番号付きの-cat-コマンドの作成date : 2023_Jun7coder : name : 武藤 太央 (Muto Tao)        studentID : 254739*/import java.io.*;       // Reader系のクラスとIOExceptionを用いるためにimportpublic class Cat2 {    void run(String[] args) throws IOException {        for(Integer i = 0; i < args.length; i ++){      // コマンドライン引数で与えられファイルを一つ一つ順番に処理する。                  cat(new File(args[i]));     // ファイルの内容を表示する。        }    }    void cat(File file) throws IOException {        // ファイルの内容を表示するメソッド        BufferedReader in = new BufferedReader(new FileReader(file));       // このメソッドの引数で与えられたfileを読み込むためのBufferedReader型の実体を作成する。        LineNumberReader lineNumber = new LineNumberReader(in);       // inを読みこむためのLineNumberReader型の実体を作成する。        String line;        // ファイルの中身を表現するための変数        while ((line = lineNumber.readLine()) != null) {        // まだ扱っていないファイルの内容がある限り処理を続ける。            System.out.printf("%d   ", lineNumber.getLineNumber());     // 今読み込んでいるファイルの行の行番号を取得し、表示する。            System.out.println(line);       // 今読み込んでいるファイルの行を表示する。        }        lineNumber.close();     // ファイルの読み込みを終了する。    }    public static void main(String[] args) throws IOException {        Cat2 cat2 = new Cat2();        cat2.run(args);    }}